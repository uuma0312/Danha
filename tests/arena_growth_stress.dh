// 아레나 청크 성장 스트레스 — 누적 할당이 초기 256MB를 한참 넘는다.
// (이전 stage0 아레나는 검사 없는 고정 bump라 이 워크로드에서 조용한 힙 손상)
xs: [i32] = []
i = 0
while i < 100000000 {
    push(xs, i)
    i = i + 1
}
print(len(xs))
print(xs[0])
print(xs[99999999])
